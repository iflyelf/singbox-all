# ==============================================================================
# Coraza WAF 基础引擎配置 (基于官方 coraza.conf-recommended 调整)
# https://github.com/corazawaf/coraza
# ==============================================================================

# -- 规则引擎开关 --------------------------------------------------------------
# On            : 拦截模式, 命中规则即阻断 (当前启用)
# DetectionOnly : 仅检测记录, 不阻断 (排查误报时可临时切换)
# Off           : 关闭
SecRuleEngine On

# -- 请求体处理 ----------------------------------------------------------------
SecRequestBodyAccess On

# 按 Content-Type 选择请求体解析器
SecRule REQUEST_HEADERS:Content-Type "^(?:application(?:/soap\+|/)|text/)xml" \
     "id:'200000',phase:1,t:none,t:lowercase,pass,nolog,ctl:requestBodyProcessor=XML"
SecRule REQUEST_HEADERS:Content-Type "^application/json" \
     "id:'200001',phase:1,t:none,t:lowercase,pass,nolog,ctl:requestBodyProcessor=JSON"
SecRule REQUEST_HEADERS:Content-Type "^application/[a-z0-9.-]+[+]json" \
     "id:'200006',phase:1,t:none,t:lowercase,pass,nolog,ctl:requestBodyProcessor=JSON"

SecRequestBodyJsonDepthLimit 1024
SecRequestBodyLimit 13107200
SecRequestBodyInMemoryLimit 131072
SecRequestBodyLimitAction Reject

SecRule REQBODY_ERROR "!@eq 0" \
    "id:'200002',phase:2,t:none,log,deny,status:400,msg:'Failed to parse request body.',logdata:'%{reqbody_error_msg}',severity:2"
SecRule MULTIPART_STRICT_ERROR "!@eq 0" \
    "id:'200003',phase:2,t:none,log,deny,status:400,msg:'Multipart request body failed strict validation.'"

# -- 响应体处理 ----------------------------------------------------------------
SecResponseBodyAccess On
SecResponseBodyMimeType text/plain text/html text/xml
SecResponseBodyLimit 524288
SecResponseBodyLimitAction ProcessPartial

# -- 数据与审计日志 ------------------------------------------------------------
SecDataDir /tmp/
SecAuditEngine RelevantOnly
SecAuditLogRelevantStatus "^(?:(5|4)(0|1)[0-9])$"
SecAuditLogParts ABIJDEFHZ
SecAuditLogType Serial
# 审计日志输出到 stdout(汇入 docker logs), 避免 worker(nginx 用户) 无权写
# /data/nginx/logs 目录(root 所有且被卷挂载覆盖)导致 WAF 初始化失败
SecAuditLog /dev/stdout
SecAuditLogFormat Native

# -- 调试日志 (排障时可调高 SecDebugLogLevel, 0=关闭 9=最详细) -----------------
SecDebugLogLevel 0
